---
name: Dynamic Approach
menu: Hook
---

import { Playground } from "docz";
import { Translator, DynamicTextHook as DynamicText } from "./components";

<Translator heading page="dynamicApproachHook" num="0" />

<Translator page="dynamicApproachHook" num="0" />

<Translator page="dynamicApproachHook" num="1" />

<Translator page="dynamicApproachHook" num="2" />

```jsx
// './i18n/index.js'
const data = {
  de: {
    text: "Hallo Welt!",
  },
  en: {
    text: "Hello World!",
  },
  es: {
    text: "!Hola Mundo!",
  },
};

export { data };
```

<Translator page="dynamicApproachHook" num="3" />

```jsx
import React from "react";
import { useI18n } from "react-i18n-kit";

import { data } from "./i18n";

const DynamicText = () => {
  const { i18n, translate } = useI18n(data, { lang: 'en' });

  return (
    <>
      <button onClick={() => translate("en")}>
        English
      </button>
      <button onClick={() => translate("es")}>
        Español
      </button>
      <button onClick={() => translate("de")}>
        Deutsch
      </button>
      <div>{i18n.text}</div>
    </>
  );
};

export { DynamicText };
```

<Translator page="dynamicApproachHook" num="4" />

<Playground>
  <DynamicText />
</Playground>
